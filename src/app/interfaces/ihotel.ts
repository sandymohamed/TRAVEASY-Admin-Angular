export interface Ihotel {

    _id: String | null,
    HotelName : String | null,
    // Icity[]
    City :  any,
    Evaluation  : Number | null,
    ImgURL:[String] | null,
    Period : Number | null,
    // Single : Number,
    // Double : Number,
    Description: String | null,
    lon:String | null,
    lat:String | null,
    Price : Number | null,
}
