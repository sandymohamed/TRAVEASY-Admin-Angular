export interface Statistics {
  airlineCount: string | number;
  bookedHolidaysCount: string | number;
  bookedHolidaysModel: string | number;
  bookedHotels: string | number;
  cityCount: string | number;
  feedbackCount: string | number;
  flightBookingCount: string | number;
  flightCount: string | number;
  hotelCount: string | number;
  usersCount: string | number;
}
